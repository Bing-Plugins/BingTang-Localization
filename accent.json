{
  "apiUrl": "https://translate.yistars.net/",
  "apiKey": "${{ env.ACCENT_APIKEY }}",
  "files": [
    {
      "namePattern": "parentDirectory",
      "format": "yaml",
      "source": "source/**/*.yml",
      "target": "target/**/%slug%/%document_path%.json"
    }
  ]
}